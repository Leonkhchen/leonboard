{
  "name": "leon-project-control-center",
  "private": true,
  "version": "0.2.0",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:local": "wrangler d1 execute leonboard-db --local --file=./schema.sql",
    "db:remote": "wrangler d1 execute leonboard-db --remote --file=./schema.sql",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "latest",
    "typescript": "latest",
    "wrangler": "latest"
  }
}
